<%= form_with model: [conversation, post], id: "form" do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this conversation from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.label :body %>
  <%= form.rich_text_area :body %>
  <%= form.submit %>
<% end %>
